%h1 #{@exercise.title}
%form{method: "post"}
  - questions_status.each do |hash|
    %p= erb "question_#{hash[:status]}".to_sym, locals: {content: hash[:question].content, answer: hash[:question].answer, hint: hash[:question].hint, attempt_number: "attempt_#{hash[:question].number}"}
  %input{type: "submit", value:"Submit"}
